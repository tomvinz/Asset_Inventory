# AWX Credentials

Store exported credential metadata here only when it is safe to commit. Never commit secrets, private keys, SNMP community strings, vault passwords, API tokens, or cloud credentials.

Use AWX credential types and encrypted fields for runtime secrets.
