# Dashboard

The dashboard will visualize inventory coverage, newly discovered assets, service exposure, certificate status, and discovery job health.

Recommended first implementation: React with a compact operations-focused layout backed by the FastAPI `/assets` endpoint.
